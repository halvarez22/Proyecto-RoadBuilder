# Assets documentales (brochures / PDF)

Material comercial **fuera del runtime** de la SPA.

- En `main`: solo placeholders o enlaces; no bloquear el sitio.
- Histórico completo de brochures del monorepo: rama git `archive/static-legacy` (carpetas `brochures/`, `presentacion/`, etc.).

No colocar aquí API keys ni `.env`.
